export class GunModel {
    constructor(
        public id: string,
        public name: string,
        public shortDescription: string,
        public description: string,
        public url: string
    ) {
    }
}
